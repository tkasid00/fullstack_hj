#1. doAction 메서드 생성 -> doPost, doGet 메서드도 doActon(request, response); 로 교체.

#2. 메서드 내 기본 세팅
	- 글꼴 깨짐 방지 : 	request.setCharacterEncoding("UTF-8");
		              response.setContentType("text/html; charset=UTF-8");
	- 경로 설정 관련 :  String path=request.getServletPath();
	- out 구문 사용 설정 :  PrintWriter out = response.getWriter();
	- 기타 설정 : out.println(path); ReService service = null; 

#2. if 이용하여 경로별 역할 설정
		if(path.equals("/list.do")) {
		//	[전체글보기] list.do           ■ ReList 불러오고 /  ReBoard/list.jsp로 넘김            <- 사전 mvc2 설계도 참조
			service = new ReList();  service.exec(request, response);                          <- 1. 필요한 서비스 실행
			request.getRequestDispatcher("/reviewboard/list.jsp").forward(request, response);  <- 2. 경로 설정 
			
		}else if(path.equals("/writeView.do")){	
		//ㄴ[글쓰기폼]   /writeView.do      □ 기능X 단순 폼 소환 / ReBoard/write.jsp	            
			service =  new FoodList(); service.exec(request, response);                         <-0. 서비스(foodDto 분리 상황이라 소환)
			request.getRequestDispatcher("/reviewboard/write.jsp").forward(request, response);  <-1. 경로

		//	ㄴ[글쓰기기능] /write.do          ■ ReInsert         /  list.do 
		}else if(path.equals("/write.do")){
		    service = new ReInsert(); service.exec(request, response);                              <- 1. 서비스

		    String result = (String)request.getAttribute("result");                                 <- 2. int result값 받아오고 Sting으로 변환해서 넘김 

		    response.setContentType("text/html; charset=UTF-8");                                    <- 3. 글꼴 깨짐 재방지   

		    if("1".equals(result)) {
		        out.println("<script>alert('글쓰기에 성공했습니다.'); location.href='list.do';</script>");   <- 4. 글쓰기 성공, 실패 여부 판단 문구
		    } else {
		        out.println("<script>alert('관리자에게 문의바랍니다.'); location.href='list.do';</script>");
		    }

		    out.flush();                                                                                  <- 4-2. 이상 없는데 값이 제대로 넘어가질 않아서 삽입해봄.      
		    out.close();
		    
		}else if(path.equals("/editView.do")){
		//ㄴ[글수정폼]   /editView.do       ■ ReUpdateView   /  ReBoard/edit.jsp	
			service = new FoodList(); 	service.exec(request, response);                             <- 1. 서비스(food 분리로 2개 필요)
			service = new ReUpdateView();		service.exec(request, response);
			
			request.getRequestDispatcher("/reviewboard/edit.jsp").forward(request, response);        <- 2. 경로 설정
			
		}else if(path.equals("/edit.do")){
		// ㄴ[글수정기능] /edit.do           ■ ReUpdate       /  알림창 + ReBoard/detail.jsp 
			service= new ReUpdate();  service.exec(request, response);                               <- 1. 서비스
			String result = (String)request.getAttribute("result");                                  <- 2. int result값 받아오고 Sting으로 변환해서 넘김 
			int reviewid = Integer.parseInt(request.getParameter("reviewid"));                       <- 3. reviewid로 특정해서 수정할 글 선택
			
		    if("1".equals(result)) {
		        out.println("<script>alert('수정 완료'); location.href='list.do';</script>");          <- 4. 수정 성공, 실패 여부 판단 문구
		    } else {
		        out.println("<script>alert('비밀번호를 확인해 주세요'); history.go(-1);</script>");
		    }

		    
		}else if(path.equals("/deleteView.do")){
			service = new ReDeleteView(); service.exec(request, response);                          <- 1. 서비스
			request.getRequestDispatcher("/reviewboard/delete.jsp").forward(request, response);     <- 2. 경로 설정
			

		}else if(path.equals("/delete.do")){
		//ㄴ[글삭제기능] /delete.do         ■ ReDelete       /  알림창 + list.do 
			
			service = new ReDelete(); service.exec(request, response);                              <- 1. 서비스
			String result = (String)request.getAttribute("result");                                 <- 2. int result값 받아오고 Sting으로 변환해서 넘김 
			int reviewid = Integer.parseInt(request.getParameter("reviewid"));                      <- 3. reviewid로 특정해서 삭제할 글 선택
			
		    if("1".equals(result)) {
		        out.println("<script>alert('삭제 완료'); location.href='list.do';</script>");        <- 4. 식제 성공, 실패 여부 판단 문구
		    } else {
		        out.println("<script>alert('비밀번호를 확인해 주세요'); history.go(-1);</script>");
		    }
			
		}