#1. dao 메서드

*주의 : dto와 자료형, 순서가 일치해야함
        dto using field에 매칭되는 값들이 있는지 확인.



1. selectAll : 전체 목록 출력
 - ArrayList 이용해서 메서드 제작 
 - 상세 :  
```
    public ArrayList<ReDto> selectAll(){

	   //0.	생성자 : ArrayList<ReDto> result = new ArrayList();
       
       //1. 기본 구문+ sql 세팅 

       //2. try-catch로 감싸고 드라이버 연결

       //3. pstmt : sql 구문만 받아옴(조회용, 입력자료x)

       //4. rset : executeQuery + while
                -> add 이용해서 값 넣기 : result.add(new ReDto( rset.getInt("값"))); 

       //5. return result  <- ArrayList 변수명 

    }
```            

2. select : 특정 글만 조회
- public ReDto select(int 구분값) : ReDto 통해서 전달
- 상세 : 
```
public ReDto select(int reviewid) {
     //0. 리턴값 : ReDto result = new ReDto();

     //1. 기본 구문+ sql 세팅 

     //2. try-catch로 감싸고 드라이버 연결

     //3. pstmt : sql값 받아오고 구문의 ?값 받기.
                    -> pstmt.setInt(?순서, dto.get?값());

     //4. rset : executeQuery + while
                -> add 이용해서 조회에 필요한 값 넣기 : result.add(new ReDto( rset.getInt("값"))); 
     
     //5. return result         

}

``` 

3. insert : 입력  
 - public int insert(){} 형태 -> 반환값으로 글쓰기 성공, 실패 판단
 - 상세 : 
 ```
    public int insert(ReDto dto){

        //0. int result = -1; 지정하고 출발

        //1. 기본 구문+ sql 세팅 

        //2. try-catch로 감싸고 드라이버 연결

        //3. pstmt : sql값 받아오고 구문의 ?만큼 값 입력하기.
                        -> pstmt.setString(?순서, dto.get?값());
        //4. rset : executeUpdate로 받아야됨 
                    ->if(pstmt.executeUpdate()>0) {return 1;}   
        //5. return result                       
    }

 ```

4. update : 수정(입력과 유사)
 - public int update(ReDto dto){} 형태
 - 상세  :
 ```
     public int update(ReDto dto){
        //0. int result = -1; 지정하고 출발

        //1. 기본 구문+ sql 세팅 

        //2. try-catch로 감싸고 드라이버 연결
 
        //3. pstmt : sql값 받아오고 구문의 ?만큼 값 입력하기.
                        -> pstmt.setString(?순서, dto.get?값());

        //4. rset : executeUpdate로 받아야됨 
                    -> insert와 차이점 : int presert=pstmt.executeUpdate();
				                            if(presert>0) {result = 1;} 

        //5. return result     

     }

 ```
5. delete : 삭제
- public int delete(ReDto dto){} 형태
- 상세:
```
     public int update(ReDto dto){
        //0. int result = -1; 지정하고 출발

        //1. 기본 구문+ sql 세팅 

        //2. try-catch로 감싸고 드라이버 연결
 
        //3. pstmt : sql값 받아오고 구문의 ?만큼 값 입력하기.
                        -> pstmt.setString(?순서, dto.get?값());

        //4. rset : executeUpdate로 받아야됨 
                    -> update와 동일 : int presert=pstmt.executeUpdate();
				                            if(presert>0) {result = 1;} 

        //5. return result     
     }
```