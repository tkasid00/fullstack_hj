4) mvc2
	▶model 
		1.  table (users) + sequence (user_seq)

### --1. table (users) + sequence (user_seq)
| 컬럼명       | 데이터 타입       | 제약 조건               | 설명 |
|--------------|-------------------|--------------------------|------|
| `userid`     | `NUMBER`          | `PRIMARY KEY`            | 사용자 고유 ID |
| `email`      | `VARCHAR2(200)`   | `NOT NULL`, `UNIQUE`     | 이메일 주소 |
| `nickname`   | `VARCHAR2(100)`   | `NOT NULL`               | 닉네임 |
| `password`   | `VARCHAR2(100)`   | `NOT NULL`               | 비밀번호 |
| `createdat`  | `DATE`            | `NOT NULL`               | 가입일 |

```
userid      email                   nickname        password        createdate
101         'iis07007@naver.com'    '성태훈'         '1234'          '2025-11-03'
```


### --2. table (pettype) + sequence (pet_type_seq)
| 컬럼명        | 데이터 타입       | 제약 조건        | 설명 |
|---------------|-------------------|------------------|------|
| `pettypeid` | `NUMBER`          | `PRIMARY KEY`    | 반려동물 종류 ID |
| `typename`   | `VARCHAR2(100)`   | `NOT NULL`       | 종류 이름 (강아지, 고양이 등) |

```
pettypeid       typename
1               '고양이'
```

### --3. table (pet) + sequence (pet_seq)
| 컬럼명        | 데이터 타입       | 제약 조건                                | 설명 |
|---------------|-------------------|------------------------------------------|------|
| `petid`       | `NUMBER`          | `PRIMARY KEY`                            | 반려동물 고유 ID |
| `userid`       | `NUMBER`          | `FOREIGN KEY REFERENCES user(userid)`    | 사용자 ID |
| `petname`     | `VARCHAR2(100)`   | `NOT NULL`                               | 반려동물 이름 |
| `petbreed`    | `VARCHAR2(100)`   | `NOT NULL`                                | 반려동물 종 |
| `birthdate`    | `VARCHAR2(100)`   | —                                        | 생년월일 |
| `pettypeid`  | `NUMBER`          | `FOREIGN KEY REFERENCES pettype(pet_type_id)` | 반려동물 종류 ID |




        3. dto
            [com.pawject.model] - MemberDto.java(클래스 파일)
| 컬럼명       | 데이터 타입       | 제약 조건               | 설명 |
|--------------|-------------------|--------------------------|------|
| `userid`     | `NUMBER`          | `PRIMARY KEY`            | 사용자 고유 ID |
| `email`      | `VARCHAR2(200)`   | `NOT NULL`, `UNIQUE`     | 이메일 주소 |
| `nickname`   | `VARCHAR2(100)`   | `NOT NULL`               | 닉네임 |
| `password`   | `VARCHAR2(100)`   | `NOT NULL`               | 비밀번호 |
| `createdat`  | `DATE`            | `NOT NULL`               | 가입일 |

| `pettypeid` | `NUMBER`          | `PRIMARY KEY`               | 반려동물 종류 ID |
| `typename`   | `VARCHAR2(100)`   | `NOT NULL`                 | 종류 이름 (강아지, 고양이 등) |
| `petid`       | `NUMBER`          | `PRIMARY KEY`             | 반려동물 고유 ID |
| `petname`     | `VARCHAR2(100)`   | `NOT NULL`                | 반려동물 이름 |
| `petbreed`    | `VARCHAR2(100)`   | `NOT NULL`                | 반려동물 종 |
| `birthdate`    | `VARCHAR2(100)`   | —                        | 생년월일 |





		4. dao
            [com.pawject.controller] - MemberDao.java(클래스 파일)
	▶view
		1. login.jsp
		2. join.jsp
        3. mypage.jsp

	▶controller
		1. memberController
            [ . web.xml   *.lo  ] 

       [com.pawject.controller]  - MemberController
            ㄴloginindex.jsp
                ㄴ[마이페이지]   /mypage.lo         ■ MeMypage 불러오고 /  MeBoard/mypage.jsp로 넘김
                ㄴ[로그인폼]     /loginView.lo      □ 기능X 단순 폼 소환 / MeBoard/login.jsp
                ㄴ[로그인기능]   /login.lo          ■ MeLogin       /  mypage.lo
                ㄴ[회원가입폼]   /joinView.lo       □ 기능X 단순 폼 소환 /  MeBoard/join.jsp 
                ㄴ[회원가입기능] /join.lo           ■ MeJoin       /  알림창 + MeBoard/login.jsp 
                ㄴ[로그아웃기능] /logout.lo         ■ MeLogout       /  알림창 + login.lo

            1-1. frontcontroller       web.xml       [com.pawject.controller] - MemberController
            1-2. view 연결


        2. service
        [com.company.service]
            MemberService<<interface>>
                △..MeMypage      데이터 o / select(int userid)
                △..MeLogin          -    / insert(MemberDto dto)
                △..MeLoginView   데이터 o / insert(MemberDto dto)
                △..MeJoinView    데이터 o / insert(MemberDto dto)
                △..MeJoin           -    / insert(MemberDto dto)
                △..MeLogout      데이터 o / delete(int userid)
