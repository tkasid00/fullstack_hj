-u 옵션 : setquota 

특수권한 폴더 : /usr/bin/passwd

df -sh ~ihduser : 디스크 파일 시스템의 사용 가능한 공간
du -sh ~ihduser : 디렉토리의 실제 디스크 사용량


/etc/shells: 시스템에서 사용 가능한 셸의 목록을 포함
/etc/passwd: 시스템에 등록된 각 사용자의 계정 정보를 저장
~/.bashrc: 개인 사용자가 정의한 alias와 함수들이 있는 파일
~/.bash_profile : 개인사용자의 환경설정 관련 파일

백그라운드로 실행 중인 작업목록을 물어보면 => jobs
백그라운드로 실행 중인 데몬을 물어보면 => ps

10GB 디스크 4개 사용 시 각 RAID에 대한 설명.
RAID-0 : 스트라이핑의 경우 용량 모두 사용 40GB.
RAID-1 : 미러링의 경우 절반으로 줄어듦 20GB.
RAID-5 : 1개의 디스크 패리티로 사용 30GB.
RAID-6 : 2개의 디스크 패리티로 사용 20GB.

데스크톱 환경 : 사용자가 그래픽 인터페이스를 통해 컴퓨터를 사용할 수 있도록 하는 전체 환경 (예: GNOME, KDE). - 사용자 로그인 및 세션 관리
윈도 매니저 : 윈도우의 배치와 외관을 관리하는 시스템 소프트웨어 (예: Metacity, Openbox).
디스플레이 매니저 : 로그인 화면을 제공하고 사용자 세션을 시작하는 프로그램 (예: GDM, LightDM).
위젯 : 그래픽 사용자 인터페이스에서 사용되는 작은 독립형 컴포넌트 (예: 버튼, 텍스트 박스).

Nautilus는 GNOME 데스크톱 환경에서 사용되는 파일 관리자
Metacity는 GNOME 데스크톱 환경에서 사용되는 윈도우 매니저
Mutter는 GNOME 3 데스크톱 환경에서 사용되는 윈도우 매니저/Metacity 후속 버전
Plasma는 KDE 커뮤니티에 의해 개발된 데스크탑 환경.

Sticky-Bit는 디렉토리에 부여

ls : 파일이나 디렉터리 소유자를 확인
chmod : 파일이나 디렉터리 권한을 부여
chown : 파일이나 디렉터리 소유자 변경
umask : 새로 [생성]되는 파일이나 디렉터리 권한 제한

A 시스템은 X 클라이언트가 되고, 환경변수인 DISPLAY를 변경한다.
B 시스템은 X 서버가 되고, xhost 명령을 사용해서 제어한다.

반가상화 - Xen
전가상화 - KVM

ssh [계정자명@서버IP주소]

passwd 계정관련
shadow 패스워드관련

VirtualBox : 인텔의 하드웨어 가상화 VT-x와 AMD의 AMD-V를 기반으로 전가상화를 지원한다.

디스크쿼터 순서 : quotacheck → edquota → quotaon → repquota

우선순위 높아진 상태 <
우선순위 낮아진 상태 N

5분에 실행 : 5 * * * * 
5분 주기로 실행 : */5 * * * *

ps : PRI, NI, 좀비 등 복합적인 정보 출력
top: 위 정보 없음 좀 더 심플  

dpkg -l (*소문자 L) : 설치된 패키지 목록 확인
dpkg -L ＜패키지명＞ : 해당 패키지로부터 설치된 모든 파일목록 확인

단어 치환 : % s/∖<ihd∖>/kait/g
chsh -u, --help : 사용법

데스크톱환경 : 인터페이스, 드래그앤드롭

디렉터리 용량을 늘리는 목적이면 : LVM
디스크 장애 대비나 속도가 목적이면 : RAID   

-c : 파일이나 디렉터리를 묶는것
-C : 디렉터리 변경

+ :  커서 위치 지정, 별도 지정 없으면 마지막줄에 위치함

ESTABLISHED 정보 : 	ss, netstat


Eog : 이미지 [뷰어] 프로그램
ImageMagicK : [비트맵] 이미지를 보여주고 생성 및 편집이 가능하도록 지원
Gimp : 이미지 편집, 변환, 생성 프로그램

/usr/bin/passwd -> -rwsr-xr-x (SET-UID, SET-GID의 권한 s 확인할 수 있다.)

standalone :백그라운드 [요청] 기다렸다가 요청이 오면 혼자 처리하는 방식
daemon : 프로세스로 메모리에 [상주]해 있다가 사용자 요청이 오면 응답하는 프로세스

ps –l : 우선순위(PRI, NI)
ps aux :  RSS(실제 메모리 사용량),TIME(총 CPU 사용 시간)등 

repquota : 모든 사용자의 쿼터 정보
quota : 특정 사용자의 쿼터 정보

게이트웨이 주소 확인 명령어 : route, netstat -r

IEEE 802.6 - DQDB

x윈도우 텍스트모드 systemctl set-default [multi-user].target

 vi 편집기 실행 시 자동으로 행 번호 : ㉠ .exrc, ㉡ set nu

디스크 단위 : GB＜TB＜PB＜EB

df = 디스크 여유공간 확인
du = 디스크 사용량 확인

chmod 3070 : 특수(3) 소유자(0) 그룹(7) 기타(0) 
Set-UID(4) Set-GID(2) Sticiky-bit(1)

DNS(nameserver)는 -> /etc/resolv.conf

CentOS 7 버전에서 이더넷 카드 : enp0s3
CentOS 7 이전 버전 : eth0