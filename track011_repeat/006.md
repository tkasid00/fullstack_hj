2026-03-07

**자바스크립트**
- https://ko.javascript.info/
- 자바스크립트 기본 4.5~5-1 진행

**A. new 연산자와 생성자 함수**
1. 과제
    1) new A()==new B()가 성립 가능한 함수 A와 B를 만드는 게 가능할까요?
    ```js
    function A() { ... }
    function B() { ... }

    let a = new A;
    let b = new B;

    alert( a == b ); // true
    ```
    ```js
     // this 대신에 객체를 반환
    let obj = {};

    function A() { return obj; }
    function B() { return obj; }

    alert( new A() == new B() ); // true
    ```

    2) 아래와 같은 세 개의 메서드를 가진 생성자 함수, Calculator를 만들어보세요.
    - read() – prompt 함수를 이용해 사용자로부터 값 두 개를 받고, 이를 객체 프로퍼티에 저장합니다.
    - sum() – 프로퍼티에 저장된 값 두 개를 더한 후 반환합니다.
    - mul() – 프로퍼티에 저장된 값 두 개를 곱한 후 반환합니다.
    ```js
    //예시
    let calculator = new Calculator();
    calculator.read();

    alert( "Sum=" + calculator.sum() );
    alert( "Mul=" + calculator.mul() );
    ```
    ```js    
    //답
    function Calculator(){
        this.read=function(){
            this.a= +prompt("첫 번째 숫자를 입력하세요");
            this.b= +prompt("두 번째 숫자를 입력하세요");
        }
        this.sum=function(){return  this.a+this.b;}
        this.mul=function(){return this.a*this.b;}
    }
    let calculator = new Calculator();
    calculator.read();

    alert( "Sum=" + calculator.sum() );
    alert( "Mul=" + calculator.mul() );

    ```

    3) 생성자 함수 Accumulator(startingValue)를 만들어 보세요.
    - Accumulator(startingValue)를 이용해 만드는 객체는 아래와 같은 요건을 충족해야 합니다.
    - 프로퍼티 value에 현재 값(current value)을 저장합니다. 
    - 최초 호출 시엔 생성자 함수의 인수, startingValue에서 시작값(starting value)을 받아옵니다.
    - 메서드 read()에선 prompt 함수를 사용해 사용자로부터 숫자를 받아오고, 받은 숫자를 value에 더해줍니다.
    - 프로퍼티 value엔 startingValue와 사용자가 입력한 모든 값의 총합이 더해져 저장됩니다.
    ```js
    let accumulator = new Accumulator(1); // 최초값: 1

    accumulator.read(); // 사용자가 입력한 값을 더해줌
    accumulator.read(); // 사용자가 입력한 값을 더해줌

    alert(accumulator.value); // 최초값과 사용자가 입력한 모든 값을 더해 출력함
    ```
    ```js
    // 답 - value가 함수value()가 아님 주의! 
    function Accumulator(startingValue){
        this.value = startingValue;
        this.read=function(){
            this.value += +prompt("숫자를 입력하세요");
        }
    }
    let accumulator = new Accumulator(1); 
    accumulator.read(); 
    accumulator.read(); 
    alert(accumulator.value); 
    ```

    <br/>  <br/>  <br/>

    **자바**
- https://school.programmers.co.kr/learn/challenges?order=recent&levels=0&languages=java

- 문자열 겹쳐쓰기
```java
    //문자열 자르고 붙여서 해결. 이게 맞나...
    class Solution {
        public String solution(String my_string, String overwrite_string, int s) {
            String answer = "";
                String my = my_string.substring(0, s);
                String my2 = my_string.substring(my.length()+overwrite_string.length()); 
                
                answer = my.concat(overwrite_string).concat(my2);
            return answer;
        }
    }

    //반복문 사용 버전
    class Solution {
        public String solution(String my_string, String overwrite_string, int s) {
            String answer = "";
            char[] my = my_string.toCharArray();
            char[] over = overwrite_string.toCharArray();
            
            for (int i = 0; i < over.length; i++) { //my로 기준 잡으면 마이너스배열값 나와서 안됨
                my[s + i] = over[i]; 
            } answer = new String(my);

            return answer;
        }
    }

```

- 문자열 섞기
```java
class Solution {
    public String solution(String str1, String str2) {
        String answer = "";
        char[] strA = str1.toCharArray();
		char[] strB = str2.toCharArray();
		char[] arr = new char[strA.length+strB.length];
        //반복문 한 번만 사용
		for (int i=0; i <arr.length; i++) {
		    if (i%2 == 0) {arr[i] = strA[i/2];}
		    else { arr[i] = strB[i/2];}
		} answer = new String(arr);
        return answer;
    }
}
```

- 문자 리스트를 문자열로 변환하기
```java
//이것도 맞나 싶긴 한데...
class Solution {
    public String solution(String[] arr) {
        String answer = "";
        for(int i =0; i<arr.length; i++) {
			answer += "".concat(arr[i]);
		}
        return answer;
    }
}
```

- 문자열 곱하기
```java
class Solution {
    public String solution(String my_string, int k) {
        //같은 원리 도입
        String answer = "";
        for(int i =0; i<k; i++) {
			answer += "".concat(my_string);
		}
        return answer;
    }
}
```

- 더 크게 합치기
```java
class Solution {
    public int solution(int a, int b) {
        int answer = 0;
        String cal1 = a+""+b;
		String cal2 = b+""+a;
		
		int n1 = Integer.parseInt(cal1);
		int n2 = Integer.parseInt(cal2); 
        
        answer = (n1>n2)? n1:n2;
        return answer;
    }
}
```
- 두 수의 연산값 비교하기
```java
class Solution {
    public int solution(int a, int b) {
        int answer = 0;
        String cal = a+""+b;
		int n1 = Integer.parseInt(cal); //+같을 시 리턴
        int n2 = 2*a*b;
		
        answer = (n2>n1)? n2 : n1;
        return answer;
    }
}
```
- n의 배수
```java
class Solution {
    public int solution(int num, int n) {
        int answer =  (num % n == 0) ? 1 : 0;
        return answer;
    }
}
```

- 공배수
```java
class Solution {
    public int solution(int number, int n, int m) {
        int answer = (number % n == 0 && number % m == 0)? 1 : 0;
        return answer;
    }
}
```
- 홀짝에 따라 다른 값 반환하기
```java
class Solution {
    public int solution(int n) {
        int answer = 0;
		if(n%2==0) { //짝수
			for(int i=2; i<=n; i+=2) {
				answer += (i*i);
			}
		}else { //홀수
			for(int i=1; i<=n; i+=2) {
				answer +=i;
			}
		}
        return answer;
    }
}
```
- 조건 문자열
```java
class Solution {
    public int solution(String ineq, String eq, int n, int m) {
        int answer = 0;
        boolean result = false;
        //연산자 자체는 자료형이 없음.
		if(eq.equals("!")) {
			result = ineq.equals("<")? n<m : ineq.equals(">") ? n>m : false;  
		}else {
			result = ineq.equals("<")? n<=m : ineq.equals(">") ? n>=m : false;  
		}
		
		answer = result? 1:0;
        return answer;
    }
}
```

- flag에 따라 다른 값 반환하기
```java
class Solution {
    public int solution(int a, int b, boolean flag) {
        int answer = 0;
        answer = flag? a+b:a-b;
        return answer;
    }
}
```